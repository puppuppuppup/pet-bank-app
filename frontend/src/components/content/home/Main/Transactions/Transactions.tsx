import React from 'react';
import styles from './Transactions.module.scss';

type Props = {};

const Transactions = (props: Props) => {
    return <div className={styles.transactions}>Transactions</div>;
};

export default Transactions;
